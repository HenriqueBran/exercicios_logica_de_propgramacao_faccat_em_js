// Declarações das variáveis 
let area, base, altura;

// Seção de Comandos
console.log("Escreva a base do retângulo:");
base = parseInt(prompt());

console.log("Escreva a altura do retângulo:");
altura = parseInt(prompt());

// Operação
area = base * altura;

// Saída
console.log("A área do retângulo é:", area);
